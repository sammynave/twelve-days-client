{{#each lines as |line index|}}
  <hbox class="lyrics-chooser-line">
    {{#if line}}
      {{line-presenter timer=timer line=line lineIndex=index choose=(action "choose")}}
    {{else}}
      <br>
    {{/if}}
  </hbox>
{{/each}}
